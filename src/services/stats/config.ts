import { DefaultStatsConfig } from './types.js';

export const STATS_CONFIG: DefaultStatsConfig = {
  DEFAULT_VALUES: {
    points: 0,
    xp: 0,
    level: 1,
    streak: 0,
    bestStreak: 0,
    completedChallenges: 0,
    averageScore: 0,
    totalTimeSpent: 0,
    pointsToNextLevel: 1000
  }
}; 